'use server'
import { format } from 'date-fns'
import { redirect } from 'next/navigation'

export const processView = async (params, formData: FormData) => {}
export const processList = async (params, formData: FormData) => {}
export const processDownLoad = async (params, formData: FormData) => {}
export const processThumbnail = async (params, formData: FormData) => {}
export const processGeneration = async (params, formData: FormData) => {}
