'use server'
import { format } from 'date-fns'
import { redirect } from 'next/navigation'

export const processWrite = async (params, formData: FormData) => {}
export const processView = async (params, formData: FormData) => {}
export const processList = async (params, formData: FormData) => {}
export const processExpurgation = async (params, formData: FormData) => {}
